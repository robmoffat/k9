import { shape, render, timeline, play } from '../libraries/wilderness.js' 

// Handles the transition between one SVG page and the next.

export function replace(xml) {
	
	alert("starting transition")
}